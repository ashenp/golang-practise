请编写一个函数，该函数接受两个参数：一个字符串切片和一个字符串。该函数的作用是将字符串切片中的所有元素连接起来，以指定的字符串为分隔符，返回连接后的字符串。例如，如果字符串切片为 ["foo", "bar", "baz"]，分隔符为 "|"，则函数应该返回 "foo|bar|baz"。
请注意，该函数需要处理字符串切片为空的情况，并且分隔符也可能为空字符串。你需要使用 golang 的 string、strings 包和函数。