任务描述：

编写一个文件读写程序，能够读取并解析 CSV 文件，并将结果输出为 JSON 或者 XML 格式。

实现要求：

1. 程序从命令行接受一个 CSV 文件路径和一个输出格式（JSON 或者 XML），并将结果输出到标准输出流。
2. CSV 文件的格式为：每行包含若干个字段，使用逗号分隔符分割，每个字段可以使用双引号括起来。
3. 程序应该将 CSV 文件的数据转换为一个内存数据结构（例如一个二维数组或者一个结构体数组），并使用 encoding/json 或者 encoding/xml 包来将其转换为对应的格式输出。
4. 程序应该处理错误情况，例如文件不存在、文件格式不正确等，并给出有意义的提示信息。
